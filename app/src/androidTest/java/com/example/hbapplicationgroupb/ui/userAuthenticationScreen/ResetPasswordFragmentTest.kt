package com.example.hbapplicationgroupb.ui.userAuthenticationScreen

import androidx.test.espresso.Espresso
import androidx.test.espresso.assertion.ViewAssertions
import androidx.test.espresso.matcher.ViewMatchers
import androidx.test.ext.junit.rules.ActivityScenarioRule
import androidx.test.ext.junit.runners.AndroidJUnit4
import com.example.hbapplicationgroupb.MainActivity
import com.example.hbapplicationgroupb.R
import org.junit.Rule
import org.junit.Test
import org.junit.runner.RunWith

@RunWith(AndroidJUnit4::class)
class ResetPasswordFragmentTest{
/**    @get:Rule
    val activityRule: ActivityScenarioRule<MainActivity> =
        ActivityScenarioRule(MainActivity::class.java)

    @Test//Test New Password  field in Reset Password Screen
    fun test_isResetPasswordScreenUserNewPasswordFieldVisible(){
        Espresso.onView(ViewMatchers.withId(R.id.tv_reset_password))
            .check(ViewAssertions.matches(ViewMatchers.isDisplayed()))
    }

    @Test//Test Confirm Password  field in Reset Password Screen
    fun test_isResetPasswordScreenUserConfirmPasswordFieldVisible(){
        Espresso.onView(ViewMatchers.withId(R.id.tv_confirm_password))
            .check(ViewAssertions.matches(ViewMatchers.isDisplayed()))

    }

    @Test//Test Reset Password button field in Reset Password Screen
    fun test_isResetPasswordButtonClickable(){
        Espresso.onView(ViewMatchers.withId(R.id.btn_reset))
            .check(ViewAssertions.matches(ViewMatchers.isClickable()))
    }*/

}